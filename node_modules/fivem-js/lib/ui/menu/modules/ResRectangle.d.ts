import { Rectangle } from '../../';
import { Color } from '../../../utils';
export declare class ResRectangle extends Rectangle {
    constructor(pos: any, size: any, color: any);
    draw(offset?: any): void;
    draw(pos: any, size: any, color: Color): void;
}
